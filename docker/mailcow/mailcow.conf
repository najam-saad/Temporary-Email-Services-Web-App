# ------------------------------
# Mail Server Configuration
# ------------------------------
MAILCOW_HOSTNAME=mail.tempfreeemail.com
MAILCOW_TZ=UTC

# ------------------------------
# Database Configuration
# ------------------------------
DBNAME=mailcow
DBUSER=mailcow
DBPASS=temp_email_pass_123
DBROOT=temp_email_pass_123

# ------------------------------
# Network Configuration
# ------------------------------
HTTP_PORT=8080
HTTP_BIND=0.0.0.0
HTTPS_PORT=8443
HTTPS_BIND=0.0.0.0
SMTP_PORT=25
SMTPS_PORT=465
SUBMISSION_PORT=587
IMAP_PORT=143
IMAPS_PORT=993
POP_PORT=110
POPS_PORT=995

# ------------------------------
# SSL Configuration
# ------------------------------
SSL_TYPE=letsencrypt
SKIP_LETS_ENCRYPT=n
ADDITIONAL_SAN=

# ------------------------------
# Optional Components
# ------------------------------
SKIP_CLAMD=n
SKIP_SOLR=y
SKIP_SOGO=y
SKIP_FAIL2BAN=n

# ------------------------------
# Misc Configuration
# ------------------------------
COMPOSE_PROJECT_NAME=mailcow-dockerized
DOCKER_COMPOSE_VERSION=native
MAILCOW_BRANCH=master 